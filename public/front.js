const menuObserver=new MutationObserver(()=>{document.querySelectorAll('#menuGrid .menu-card').forEach((card,i)=>{if(card.querySelector('.food-img'))return;const img=document.createElement('img');img.className='food-img';img.src=`/assets/menu-${(i%3)+1}.svg`;img.alt='Hungry Buddy food';card.prepend(img)})});menuObserver.observe(document.getElementById('menuGrid'),{childList:true});